platform_testing:
  module:
    inputs:
      alias_name: "eks"
      deletion_window_in_days: 10
      description: "EFS KMS master key for EKS cluster"
      enable_key_rotation: "true"
      tags: {}
    outputs:
      - "alias_arn"
      - "alias_name"
      - "key_arn"
      - "key_id"
dataops_staging:
  module:
    inputs:
      alias_name: "eks"
      deletion_window_in_days: 10
      description: "EFS KMS master key for EKS cluster"
      enable_key_rotation: "true"
      tags: {}
    outputs:
      - "alias_arn"
      - "alias_name"
      - "key_arn"
      - "key_id"
dataops_dev:
  module:
    inputs:
      alias_name: "eks"
      deletion_window_in_days: 10
      description: "EFS KMS master key for EKS cluster"
      enable_key_rotation: "true"
      tags: {}
    outputs:
      - "alias_arn"
      - "alias_name"
      - "key_arn"
      - "key_id"
dataops_prod:
  module:
    inputs:
      alias_name: "eks"
      deletion_window_in_days: 10
      description: "EFS KMS master key for EKS cluster"
      enable_key_rotation: "true"
      tags: {}
    outputs:
      - "alias_arn"
      - "alias_name"
      - "key_arn"
      - "key_id"
cloudops_prod:
  module:
    inputs:
      alias_name: "automic"
      deletion_window_in_days: 10
      description: "Automic project kms key"
      enable_key_rotation: "true"
      tags: {}
    outputs:
      - "alias_arn"
      - "alias_name"
      - "key_arn"
      - "key_id"
cloudops_dev:
  module:
    inputs:
      alias_name: "cloudops"
      deletion_window_in_days: 10
      description: "Cloudops project kms key"
      enable_key_rotation: "true"
      tags: {}
    outputs:
      - "alias_arn"
      - "alias_name"
      - "key_arn"
      - "key_id"
tvx-plfm-tst2:
  module:
    inputs:
      alias_name: "eks-tst2"
      deletion_window_in_days: 10
      description: "Platform Testing2 project kms key"
      enable_key_rotation: "true"
      tags: {}
    outputs:
      - "alias_arn"
      - "alias_name"
      - "key_arn"
      - "key_id"
